import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Segurtodo.app',
  appName: 'Segurtodo',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
